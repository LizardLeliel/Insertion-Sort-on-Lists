
#include <stdio.h>

#include "compare_sorts.h"


int main(int argc, char* argv[])
{

    // Just to show that the only current sort implementation does work,
    //  will be removed soon. Uncomment it to see it.
   
    // example_list_sort(); 

    compare_sorts(50, 1, 5);

    return 0;
}